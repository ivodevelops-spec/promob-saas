// netlify/functions/contact.ts — Formulario de contacto (validación + honeypot + rate-limit). W4.
import { notImplemented, type NetlifyHandler } from "../../src/netlify";

const handler: NetlifyHandler = () => notImplemented("W4");

export default handler;
