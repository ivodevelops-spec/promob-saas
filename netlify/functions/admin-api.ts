// netlify/functions/admin-api.ts — API del panel admin (Supabase Auth). W3.
import { notImplemented, type NetlifyHandler } from "../../src/netlify";

const handler: NetlifyHandler = () => notImplemented("W3");

export default handler;
