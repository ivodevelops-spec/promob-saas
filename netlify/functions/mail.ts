// netlify/functions/mail.ts — Envío de emails (Resend). W2.
import { notImplemented, type NetlifyHandler } from "../../src/netlify";

const handler: NetlifyHandler = () => notImplemented("W2");

export default handler;
