import "./style.css";

const app = document.getElementById("app");

if (app) {
  app.innerHTML = `
    <main class="admin">
      <h1>Promob — Panel de administración</h1>
      <p>SPA en construcción (Wave 3).</p>
    </main>
  `;
}
