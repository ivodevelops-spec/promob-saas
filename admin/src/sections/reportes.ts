import { emptyState } from "../ui";

/** Reportes: ventas mensuales y exportación a Excel (próxima entrega). */
export function renderReportes(): HTMLElement {
  return emptyState({
    title: "Sección en preparación",
    text: "Disponible en la próxima entrega.",
  });
}
